name: finance_tracker
description: "Application for tracked income and expenses"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  go_router: ^17.1.0
  get_it: ^9.2.1
  flutter_bloc: ^9.1.1
  drift: ^2.31.0
  path_provider: ^2.1.5
  drift_flutter: ^0.2.8
  fpdart: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: ^2.4.13
  drift_dev: ^2.31.0

flutter:

  uses-material-design: true